/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ProductsMiniApp from './src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ProductsMiniApp);
