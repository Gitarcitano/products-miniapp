/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DashboardMiniApp from './src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => DashboardMiniApp);
